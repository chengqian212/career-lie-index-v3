# 本周项目进度汇报

## 一、本周主要工作概述

本周主要围绕“多 Agent 职业身份一致性检测系统 v3.0”进行了模型判断机制、云端部署、数据存储、风险计分、追问策略和前端监控页面的多轮优化。根据老师文档中的建议，我首先给系统加入了 `severity` 和 `confidence` 两个判断维度，用来替代原先“节点直接输出一个风险分数”的方式。原本 `quick_preanalysis_node.py` 和四个专家节点都会直接给出分数，但这会把“风险严重程度”和“系统判断的确定程度”混在一起，导致一些场景下不够合理。例如，有些线索本身不严重但判断很确定；也有些线索一旦成立会比较严重，但当前证据还不足。因此现在系统会先输出严重度和置信度，再由统一公式换算风险值。

其次，我把项目部署到了云端，使用 Streamlit Cloud 承载网页应用，使用 Supabase 免费版作为数据库。部署后，我将测试网页转发给身边同学和朋友进行小规模测试，当前一共收集了约 20 份测试数据。我根据系统判断结果和用户真实回答情况进行比对，发现系统对明显的逻辑异常、语义异常、职业常识异常、心理语言异常比较敏感，但对“回答正确但非常泛泛”和“职业身份粒度过粗”的情况识别不足。因此本周后半部分重点围绕这两类漏洞做了策略和计分规则调整。

## 二、severity 与 confidence 评分机制

原先的评分方式是让 `quick_preanalysis_node.py` 和专家节点直接输出风险分数。这个设计的问题是：模型很容易把“不确定”和“风险低”混为一谈。例如，一个回答看起来没有明显错误，但缺少足够证据时，系统不能简单地给 0 分；又比如某个回答可能存在严重异常，但当前证据不足，也不应该直接给很高分。因此我把评分机制拆成两个维度：

- `severity`：如果当前线索成立，它本身有多严重。
- `confidence`：系统对这条线索判断的确定程度。

当前系统中的严重度基础分如下：

```text
CRITICAL = 45.0
HIGH     = 25.0
MEDIUM   = 10.0
LOW      = 3.0
```

置信度权重如下：

```text
CRITICAL = 1.0
HIGH     = 0.8
MEDIUM   = 0.5
LOW      = 0.2
```

单条证据的有效风险值计算公式为：

```text
risk_value = severity_base_score * confidence_weight
```

例如：

```text
LOW + HIGH       = 3.0 * 0.8  = 2.4
MEDIUM + HIGH    = 10.0 * 0.8 = 8.0
HIGH + HIGH      = 25.0 * 0.8 = 20.0
CRITICAL + HIGH  = 45.0 * 0.8 = 36.0
```

多条独立风险线索不会简单相加，而是使用独立风险叠加公式计算总分：

```text
LieIndex = 100 * (1 - product(1 - risk_value / 100))
```

这样做的好处是：多个弱风险会共同抬高总体风险，但不会无限线性累加；多个不同来源的证据也能形成更稳定的综合判断。

## 三、云端部署与数据存储

本周完成了项目云端部署：

- 前端部署平台：Streamlit Cloud
- 数据库：Supabase 免费版
- 代码仓库：`career-lie-index-v3`
- 主入口文件：`v3/app.py`
- 模型接口：DeepSeek 官方 API，当前使用 DeepSeek V3
- 数据表：Supabase 中的 `output_files`

部署后，我对 Supabase 写入逻辑也做了调整。最开始系统会同时上传 `outputs/logs`、`outputs/reports` 以及 `.md` 文件，后来发现日志和报告中存在重复信息，而且 `.md` 文件不适合后续结构化分析。因此现在系统只上传 `outputs/reports` 下的 JSON 文件，不再上传 logs 目录和 markdown 文件。这样 Supabase 中每条记录都可以直接通过 `content_json` 查询对话轮次、最终报告、风险分数等字段，后续做统计更方便。

## 四、小规模测试发现的问题

目前一共约 20 份测试数据。根据系统判断结果和用户真实回答情况进行初步比对后，统计结果暂时保留如下：

```text
总测试数据：20 份
有意捏造假身份：xx 份
真实身份回答：xx 份
假身份中被系统检测出风险：xx 份
```

这里的 `xx` 先保留，因为具体真假身份数量和系统命中数量还需要继续人工复核。

测试中已经能被系统检测出的风险，通常都有比较明显的异常，例如：

- 逻辑前后不一致
- 语义表达冲突
- 职业常识不符合
- 心理语言层面的异常表达
- 对关键问题进行回避

但还有两类问题比较明显。

第一类是“正确但泛泛”的回答。比如用户假装自己是心理咨询师，系统问“你平时一天都干什么呀”，对方回答“疏导病人情绪，解开心结，和他们聊天”。这句话从表面上看并没有错，也符合心理咨询师的一般印象，但它缺少真实经验中通常会出现的流程、对象、边界、记录、转介、督导、复盘等具体信息。没有做过该职业的人也很容易说出这种泛泛的正确话，因此系统不能简单认为这是低风险。

第二类是“职业粒度过粗”。比如用户只说“我是公务员”，但公务员本身包含窗口服务、基层事务、执法、综合材料、税务、公安、法院、检察、街道、机关办公室等很多类型。如果系统不先问清具体岗位方向，而是直接问“你怎么处理日常公务”，用户就可以继续用“服务群众、处理事务、完成本职工作”这类空泛表达蒙混过去。

## 五、正确但泛泛回答的处理

针对“正确但泛泛”的问题，我在 `quick_preanalysis_node.py` 中加入了经验密度相关字段，用来判断用户回答是否真的包含职业经历痕迹：

```text
specificity_level
experience_density
generic_answer_flag
generic_answer_reason
suggested_probe_angle
generic_answer_streak
generic_answer_count
```

这些字段的含义如下：

- `specificity_level`：回答的具体程度，判断是否出现了具体对象、流程、场景、工具、边界或结果。
- `experience_density`：回答中的经验密度，判断是否有真实经历中的细节痕迹。
- `generic_answer_flag`：当前回答是否属于“正确但泛泛”。
- `generic_answer_reason`：为什么判断为泛泛回答。
- `suggested_probe_angle`：下一轮建议从哪个经验角度追问。
- `generic_answer_streak`：连续泛泛回答的轮数。
- `generic_answer_count`：累计泛泛回答的次数。

目前经验追问角度已经并入原本的 `followup_strategy`，包括：

```text
process_sequence      过程顺序
boundary_judgment     边界判断
real_constraint       真实限制
counterexample        反例和卡点
term_clarification    术语澄清
output_evidence       产出证据
```

当系统发现当前回答符合常识但经验密度不足时，不会立刻调用专家分析，也不会直接判定为高风险，而是会把这个信号传给后续的 `risk_aggregator_node.py` 和 `strategy_supervisor_node.py`。策略节点会优先继续追问，并且换一个经验角度，而不是重复问同一个意思的问题。

## 六、泛泛回答的计分规则

在 `risk_aggregator_node.py` 中，我新增了经验密度风险事件。当前规则如下：

```text
当前回答泛泛，且 experience_density = LOW：
risk_value = 8.0

当前回答泛泛，且连续泛泛 >= 2 轮：
risk_value = 16.0

当前回答泛泛，且连续泛泛 >= 3 轮，或累计泛泛 >= 4 次：
risk_value = 20.0

当前回答泛泛，且连续泛泛 >= 4 轮，或累计泛泛 >= 5 次：
risk_value = 24.0
```

后续又发现一个问题：如果前几轮用户一直泛泛回答，但最后一轮突然说出一个具体时间或具体细节，系统不应该把之前的风险完全清零。因此我加入了“历史残余风险”规则。也就是说，后续具体回答可以降低之前泛泛回答造成的风险，但不能完全抹掉历史模式。

当前残余风险计算规则如下：

```text
如果累计泛泛 >= 5 次：
base = 18.0

如果累计泛泛 >= 3 次：
base = 16.0

如果累计泛泛 = 2 次：
base = 12.0

如果累计泛泛 = 1 次：
base = 8.0
```

然后根据本轮具体程度进行折扣：

```text
specificity_level = HIGH   -> factor = 0.65
specificity_level = MEDIUM -> factor = 0.8
specificity_level = LOW    -> factor = 1.0
```

最终残余风险为：

```text
residual_risk = max(3.0, base * factor)
```

例如，用户前面累计泛泛 3 次，最后一轮回答具体程度为 HIGH，则残余风险为：

```text
16.0 * 0.65 = 10.4
```

这样可以避免“前面一直空泛，最后补一个细节，分数直接归零”的问题。

## 七、职业粒度过粗的处理

针对“公务员”“老师”“医生”“心理咨询师”“程序员”等宽泛职业类别，我在 `quick_preanalysis_node.py` 中加入了职业粗粒度识别逻辑。当用户只说出一个大职业类别，但没有给出具体系统、岗位性质、职责方向、服务对象或业务内容时，系统会生成一个 `role_disambiguation` 信号。

这个信号的作用不是直接加风险分，而是提醒系统先澄清身份粒度。目前这个异常项中设置了：

```text
type = role_disambiguation
severity = LOW
confidence = HIGH
risk_value = 0.0
```

这里把 `risk_value` 设置为 0，是因为“职业说得宽泛”本身不等于说谎。它更像是追问方向信号，而不是风险证据。后续 `lightweight_routing_supervisor_node.py` 会识别这个信号，并跳过专家分析，直接进入轻量澄清追问：

```text
followup_strategy = light_clarification
```

在 `followup_generation_node.py` 中，我也做了限制：遇到职业粒度过粗时，系统不能直接问具体工作流程，而应该先问清职业类型、系统、岗位性质或职责方向。这样可以避免用户在身份还没有澄清的情况下，用空泛话术继续回答。

## 八、简短回答误判问题

测试中还发现一个新问题：只要用户回答得简短，系统就容易把它判成泛泛回答并加风险分。但有些简短回答本身是有效的，比如用户回答“大学生，大三，计算机专业的”，虽然很短，但已经给出了身份、年级和专业，不能因为短就加风险。

因此我在 `quick_preanalysis_node.py` 中加入了“简短但有效回答”的保护逻辑。当前思路是：

- 如果回答很短，但提供的是身份、职业、公司、年级、阶段等基础事实，则不作为泛泛风险。
- 如果回答很短，但包含具体工作对象或具体业务内容，也不直接作为风险。
- 如果回答只是方法层面的常识口号，比如“先听他说”“先安抚”“顺着他说”，虽然不一定错，但经验密度不足，可以继续作为泛泛回答处理。

这样可以区分“短但有效”和“短且空泛”。前者不应加分，后者仍然需要追问。

## 九、监控页面与日志修复

本周还修复了 Agent 思考监控页面的多个问题。之前监控页面出现过每一轮显示同一条快速预分析、追问内容错位、最终生成报告后历史轮次被最后一轮内容覆盖等问题。原因是前端有些监控内容依赖运行中的临时状态，最终报告生成时又可能触发节点更新，导致历史记录被污染。

现在我对 `app.py` 做了以下调整：

- 每轮结束时，将该轮的快速预分析、路由决策、风险聚合、策略决策、追问生成等内容保存成独立 `round_record`。
- 保存时使用深拷贝冻结 `agent_thoughts`，避免后续轮次修改同一对象。
- 监控页面显示时，不再完全信任临时 `agent_thoughts`，而是优先根据每轮 `round_record` 中的快照字段重建展示内容。
- 最终报告生成时只调用 `report_generation_node`，不再重新跑完整图，避免最后一轮覆盖前面轮次。
- 取消“最近轮次概述”模块，只保留左侧轮次列表和当前轮次详情。

现在新产生的轮次记录会固定保存当轮节点分析内容，最终报告生成后不会再把所有轮次改成最后一轮。

## 十、追问生成优化

测试中发现，模型追问容易出现两个问题：一是重复上一轮问题，二是每句话开头都像“哦，xxx”，显得机械。为此我对 `followup_generation_node.py` 和 prompts 做了优化：

- 收集历史 AI 问题，避免生成和前面同义的问题。
- 不只判断字面重复，也判断问题意图是否重复。
- 如果模型生成的问题重复，系统会改用备用追问。
- 增加开头多样性处理，减少每轮都用“哦”“哦哦”“原来”“哈哈”等固定开头。
- 增加低温润色步骤，让问题更自然，但不改变追问目标。

同时，我删除了之前针对个别错词的硬编码补丁，例如“不要写成某个词”这类只能解决单个错误的规则。现在主要通过整体追问润色、语境约束和问题复核来改善模型表达，而不是堆单词黑名单。

## 十一、当前效果

修改后，系统整体有以下变化：

1. 对明显逻辑、语义、职业常识和心理语言异常仍然能正常识别。
2. 对“正确但泛泛”的回答不再轻易给 0 分，而是会累计经验密度风险。
3. 如果用户前几轮一直泛泛，后面突然变具体，风险会下降但不会直接清零。
4. 对宽泛职业类别会先澄清具体岗位方向，而不是过早进入工作流程问题。
5. 简短但有效的身份类回答不会因为字数少被直接加风险。
6. 追问更关注真实经历中的流程、边界、限制、产出和卡点。
7. 监控页面能按轮次展示对应节点分析，不再在最终报告后覆盖历史轮次。

整体来看，系统从“判断回答是否表面正确”进一步转向“判断回答是否包含真实经历痕迹”。这比单纯找错误更适合应对有意捏造身份但会说正确套话的测试者。

## 十二、后续计划

下一步计划主要包括：

1. 对当前 20 份测试数据进行人工复核，补全真实身份、假身份、系统命中情况等统计。
2. 继续观察经验密度风险权重是否合适，避免对真实用户的简短回答误伤。
3. 增加更多职业类型测试，尤其是公务员、教师、医生、心理咨询师、程序员等容易被泛泛回答蒙混的职业。
4. 继续优化追问自然度，让系统既能逼近真实经验，又不显得像审问或面试。
5. 基于 Supabase 中的 JSON 报告，整理出可量化的数据分析表，包括每轮对话、最终报告、风险分、是否假身份、是否命中等字段。
6. 根据后续数据结果继续调整 `risk_aggregator_node.py` 中的经验密度残余风险和持续模糊表达权重。
