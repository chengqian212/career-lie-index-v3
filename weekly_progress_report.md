# 本周项目进度汇报

## 一、本周主要工作概述

本周主要围绕多 Agent 职业身份一致性检测系统 v3 进行了三方面工作：

1. 根据老师文档建议，重构系统的风险评分方式，引入 `severity` 和 `confidence` 两个标签，替代原先各节点直接输出单一分数的方式。
2. 将项目部署到云端，使用 Streamlit Cloud 承载前端页面，使用 Supabase 作为日志与报告数据存储数据库，并邀请身边同学和朋友进行小规模测试。
3. 基于 20 份测试数据，分析系统在真实身份与捏造身份场景下的判断效果，发现系统对明显异常较敏感，但对“正确但泛泛”的虚假回答和“职业粗粒度”问题识别不足，因此进一步优化了追问策略和风险聚合规则。

## 二、引入 severity 和 confidence 的评分机制

原本系统中的 `quick_preanalysis_node.py` 和四个专家节点会直接输出风险分数。但在实际测试中，这种方式不够合理，因为风险判断至少包含两个维度：

- `severity`：当前线索如果成立，问题有多严重。
- `confidence`：系统对当前线索判断的确定程度。

例如，某个回答可能看起来风险较低，但系统也不确定；也可能某个线索本身较严重，但证据还不够充分。如果只输出一个分数，这两种情况很难区分。因此我把评分机制改为先输出 `severity` 和 `confidence`，再由统一的计分函数换算为风险值。

当前系统中，`severity` 的基础分为：

```text
CRITICAL = 45.0
HIGH     = 25.0
MEDIUM   = 10.0
LOW      = 3.0
```

`confidence` 的权重为：

```text
CRITICAL = 1.0
HIGH     = 0.8
MEDIUM   = 0.5
LOW      = 0.2
```

单条证据的风险值计算公式为：

```text
risk_value = severity_base_score * confidence_weight
```

例如：

```text
LOW + HIGH       = 3.0 * 0.8  = 2.4
MEDIUM + HIGH    = 10.0 * 0.8 = 8.0
HIGH + HIGH      = 25.0 * 0.8 = 20.0
CRITICAL + HIGH  = 45.0 * 0.8 = 36.0
```

多个风险事件不会简单相加，而是使用独立风险叠加公式计算总分：

```text
LieIndex = 100 * (1 - product(1 - risk_value / 100))
```

这样可以避免多个弱信号无限线性叠高，同时也能让多个独立风险线索共同提高最终风险指数。

## 三、云端部署与数据收集

本周我将项目部署到了云端：

- 前端部署：Streamlit Cloud
- 数据库：Supabase 免费版
- 主入口文件：`v3/app.py`
- 数据存储：将系统生成的 JSON 报告同步到 Supabase，便于后续分析测试结果

部署完成后，我将网页转发给身边同学和朋友进行测试。目前共收集了 20 份小规模测试数据。

根据系统判断结果和用户真实回答情况进行初步比对后，当前统计结果如下：

```text
总测试数据：20 份
有意捏造假身份：xx 份
真实身份回答：xx 份
假身份中被系统检测出风险：xx 份
```

这里的 `xx` 目前保留，因为具体数量还需要进一步人工统计和复核。

初步观察发现，系统能够检测出一部分明显异常。这些被检测出来的样本通常存在比较明显的：

- 逻辑异常
- 语义前后不一致
- 职业常识冲突
- 心理语言层面的异常表达

但是仍然有一部分假身份没有被有效识别。

## 四、测试中发现的问题

### 1. 正确但泛泛的虚假回答

测试中发现，有些测试者虽然在捏造身份，但他们的回答并不直接出错，而是使用非常泛泛、正确但缺少细节的表达。

例如用户声称自己是心理咨询师，系统询问：

```text
你平时一天都干什么？
```

用户可能回答：

```text
疏导病人情绪，解开心结，和他们聊天。
```

这类回答从语义和职业常识上看并没有明显错误，但缺少真实经历中通常会出现的具体信息，例如：

- 咨询对象类型
- 一次咨询的大致流程
- 遇到情绪激动时的处理边界
- 记录、转介、督导、复盘等细节

因此原系统容易把这类回答判断为低风险。但实际上，这种回答可能是“没有真实经历的人也能说出来”的正确废话。

### 2. 职业粗粒度问题

另一个问题是职业身份粒度过粗。

例如用户只说：

```text
我是公务员。
```

但公务员本身是一个非常宽泛的职业类别，可能包括窗口服务、基层事务、综合材料、执法、税务、公安、法院、检察等不同方向。原系统没有优先追问“具体是哪一类公务员”，而是直接进入：

```text
你平时怎么处理日常事务？
```

这样会导致测试者可以继续用空泛表达蒙混过去，例如“处理公务、服务群众、做好本职工作”等。整个对话会变成假大空，但系统不一定能给出足够高的风险判断。

## 五、针对问题做出的改动

### 1. 对“正确但泛泛回答”的处理

我在 `quick_preanalysis_node.py` 中加入了经验密度相关字段，用于判断当前回答是否属于“正确但泛泛”：

```text
specificity_level
experience_density
generic_answer_flag
generic_answer_reason
suggested_probe_angle
generic_answer_streak
generic_answer_count
```

其中：

- `experience_density` 用来判断回答中是否有真实经历痕迹。
- `generic_answer_flag` 用来标记回答是否正确但泛泛。
- `generic_answer_streak` 用来记录连续泛泛回答轮数。
- `generic_answer_count` 用来记录累计泛泛回答次数。
- `suggested_probe_angle` 用来提示下一轮从哪个经验角度继续追问。

当用户的回答符合常识但缺少具体场景、流程、边界、限制、产出等信息时，系统不再简单认为“无风险”，而是会进入经验链追问。

目前经验链追问角度包括：

```text
process_sequence      过程顺序
boundary_judgment     边界判断
real_constraint       真实限制
counterexample        反例和卡点
term_clarification    术语澄清
output_evidence       产出证据
```

这让系统不再只问“你平时做什么”，而是更关注用户是否真的能说出实际经历中的过程、判断和限制。

在 `risk_aggregator_node.py` 中，我也调整了相关计分规则。对于正确但泛泛的回答，现在会形成独立的经验密度风险信号：

```text
单轮低经验密度：
risk_value = 8.0

连续泛泛 >= 2 轮：
risk_value = 16.0

连续泛泛 >= 3 轮，或累计泛泛 >= 4 次：
risk_value = 20.0

连续泛泛 >= 4 轮，或累计泛泛 >= 5 次：
risk_value = 24.0
```

这样修改后，如果测试者持续给出“正确但泛泛”的回答，系统的风险指数会明显上升，而不会停留在 4 到 5 分左右。

### 2. 对“职业粗粒度”的处理

我在 `quick_preanalysis_node.py` 中加入了职业粗粒度识别逻辑。当用户只给出宽泛职业类别，而没有说明具体系统、岗位性质、职责方向或工作对象时，系统会补充一个低风险异常。

例如：

```text
我是公务员。
我是老师。
我是医生。
我是心理咨询师。
我是程序员。
```

如果用户没有进一步说明具体岗位方向，系统会标记：

```text
type = vague
severity = LOW
confidence = HIGH
```

这个判断不是说用户一定有问题，而是说明“职业身份粒度过粗，需要先澄清”。因此它不会直接调用职业常识专家，也不会直接进入高风险判断。

随后在 `lightweight_routing_supervisor_node.py` 中，我加入了路由规则：如果当前异常只是职业大类过宽，系统会跳过专家分析，直接进入温和澄清策略：

```text
followup_strategy = light_clarification
```

在 `followup_generation_node.py` 中，我也加入了对应限制：遇到职业粗粒度问题时，系统不会直接问“你怎么处理公务/病人/客户/项目”，而是先自然询问对方更偏哪个系统、岗位性质或职责方向。

这样可以避免系统过早进入工作流程追问，也能减少用户用空泛回答蒙混的空间。

### 3. 对持续模糊表达的计分

除了 `generic_answer_flag` 以外，我还在 `risk_aggregator_node.py` 中加入了持续模糊表达的累计计分。

当前会统计这些异常类型：

```text
vague
lack_of_detail
vague_expression
avoidance
```

如果这些异常连续出现，会产生额外的经验密度风险事件：

```text
2 条持续模糊线索：
risk_value = 12.0

3-4 条持续模糊线索：
risk_value = 18.0

5 条及以上持续模糊线索：
risk_value = 22.0
```

这部分主要用于识别“每一句都没有明显错误，但一直不落地”的情况。

## 六、修改后的效果

修改后，系统的追问方式有明显变化：

1. 系统开始更关注对方是否能说出真实经历中的细节。
2. 对正确但泛泛的回答，不再轻易判断为低风险。
3. 对职业身份粒度过粗的回答，会先追问具体岗位或职责方向。
4. 对持续模糊、回避具体业务的回答，风险分会逐步上升。

例如面对“心理咨询师”这类身份，系统不再只停留在“你一天都做什么”，而会更关注：

- 咨询过程中一般怎么开始
- 遇到情绪激动时如何判断边界
- 什么情况下需要暂停、转介或升级
- 咨询结束后是否有记录、复盘或督导

面对“公务员”这类职业大类，系统会先澄清：

- 是窗口服务还是综合材料
- 是基层事务还是执法类
- 是面向群众还是偏内部协调

整体来看，系统现在更注重用户回答中的具体经验痕迹，而不仅仅判断语义是否正确。

## 七、后续计划

下一步计划主要包括：

1. 对目前 20 份测试数据进行人工复核，补全真实身份、假身份、系统识别结果等统计。
2. 继续观察“正确但泛泛回答”的风险分是否过高或过低，并根据实际样本微调权重。
3. 优化追问生成，让问题在自然聊天和有效识别之间保持平衡，避免过于面试化。
4. 继续扩充测试样本，观察系统在不同职业身份上的泛化能力。
5. 整理 Supabase 中的历史 JSON 报告，形成更完整的数据分析表。

